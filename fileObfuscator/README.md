getchar() and putchar() are function that can use to read a file or input.

```bash
./transformer < secrect.txt > outputbinary      // incode
./transformer < outputbinary > oldsecret.txt  // decode
```
